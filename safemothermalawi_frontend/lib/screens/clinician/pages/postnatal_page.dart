import 'package:flutter/material.dart';
import '../../../theme/app_colors.dart';

class ClinicianPostnatalPage extends StatelessWidget {
  const ClinicianPostnatalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Postnatal Visits', style: TextStyle(fontSize: 24, color: AppColors.g800)),
    );
  }
}
