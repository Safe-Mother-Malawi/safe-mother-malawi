/// EventsStore is no longer used — calendar now loads from the backend API.
/// Kept as an empty stub to avoid breaking any remaining imports.
class EventsStore {
  EventsStore._();
  static final EventsStore instance = EventsStore._();
}
