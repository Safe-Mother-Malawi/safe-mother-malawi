name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
        
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        
    - name: Get dependencies
      run: flutter pub get
      
    - name: Build APK
      run: flutter build apk --release -t lib/main_mobile.dart
      
    - name: Upload APK artifacts
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: build/app/outputs/flutter-apk/app-release.apk
        retention-days: 30
        
    - name: Create Release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: Release v1.0.${{ github.run_number }}
        body: |
          ## Safe Mother Malawi - Mobile App Release
          
          ### What's Included:
          - ✅ Prenatal & Neonatal features
          - ✅ Health facilities (1000+)
          - ✅ Real-time alerts
          - ✅ Appointments
          - ✅ Health assessments
          
          ### Backend:
          - Production: https://backend-gsgb.onrender.com
          
          ### Installation:
          1. Download app-release.apk
          2. Enable "Install from Unknown Sources"
          3. Install and open "Safe Mother Malawi"
          
          ### Default Accounts:
          - Admin: admin@safemothermalawi.mw / Admin@123
          - DHO: dho@safemothermalawi.mw / Dho@123
        files: build/app/outputs/flutter-apk/app-release.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
